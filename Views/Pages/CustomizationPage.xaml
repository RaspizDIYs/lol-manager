<UserControl x:Class="LolManager.Views.Pages.CustomizationPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:converters="clr-namespace:LolManager.Converters"
             x:Name="CustomizationPageRoot">
    
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
        <converters:InverseBoolToVisibilityConverter x:Key="InverseBoolToVisibility"/>
        <converters:ChampionImageConverter x:Key="ChampionImageConverter"/>
        <converters:ChampionSplashartConverter x:Key="ChampionSplashartConverter"/>
        <converters:SkinSplashartConverter x:Key="SkinSplashartConverter"/>
        <converters:ChallengeIconConverter x:Key="ChallengeIconConverter"/>
        <converters:MultiObjectEqualityConverter x:Key="MultiObjectEqualityConverter"/>
        <converters:ImageUrlConverter x:Key="ImageUrlConverter"/>
        
        <DataTemplate x:Key="ChampionTemplate">
            <StackPanel Orientation="Horizontal">
                <Border Width="28" 
                        Height="28" 
                        CornerRadius="14"
                        Margin="0,0,6,0"
                        VerticalAlignment="Center"
                        ClipToBounds="True">
                    <Image Source="{Binding Converter={StaticResource ChampionImageConverter}}" 
                           Stretch="UniformToFill"/>
                </Border>
                <TextBlock Text="{Binding}" 
                           VerticalAlignment="Center"
                           FontSize="12"/>
            </StackPanel>
        </DataTemplate>
        
        <DataTemplate x:Key="BackgroundChampionTemplate">
            <Border CornerRadius="8"
                    Margin="4"
                    Width="200"
                    Height="120"
                    ClipToBounds="True"
                    Cursor="Hand"
                    MouseLeftButtonDown="BackgroundChampion_MouseLeftButtonDown">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Style.Triggers>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <MultiBinding Converter="{StaticResource MultiObjectEqualityConverter}">
                                        <Binding ElementName="CustomizationPageRoot" Path="ViewModel.SelectedChampionForBackground"/>
                                        <Binding Path="."/>
                                    </MultiBinding>
                                </DataTrigger.Binding>
                                <Setter Property="BorderBrush" Value="#FF6366F1"/>
                                <Setter Property="BorderThickness" Value="3"/>
                            </DataTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#FF6366F1"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Image Source="{Binding Converter={StaticResource ChampionSplashartConverter}}"
                           Stretch="UniformToFill"
                           Opacity="0.7"/>
                    <Border Background="#80000000" VerticalAlignment="Bottom">
                        <TextBlock Text="{Binding}"
                                   Foreground="White"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Margin="8,4"
                                   TextTrimming="CharacterEllipsis"/>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>
        
        <DataTemplate x:Key="BackgroundSkinTemplate">
            <Border CornerRadius="8"
                    Margin="4"
                    Width="200"
                    Height="120"
                    ClipToBounds="True"
                    Cursor="Hand"
                    MouseLeftButtonDown="BackgroundSkin_MouseLeftButtonDown">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}"/>
                        <Setter Property="BorderBrush" Value="{DynamicResource ControlStrokeColorDefaultBrush}"/>
                        <Setter Property="BorderThickness" Value="1"/>
                        <Style.Triggers>
                            <DataTrigger Value="True">
                                <DataTrigger.Binding>
                                    <MultiBinding Converter="{StaticResource MultiObjectEqualityConverter}">
                                        <Binding ElementName="CustomizationPageRoot" Path="ViewModel.SelectedSkinForBackground"/>
                                        <Binding Path="."/>
                                    </MultiBinding>
                                </DataTrigger.Binding>
                                <Setter Property="BorderBrush" Value="#FF6366F1"/>
                                <Setter Property="BorderThickness" Value="3"/>
                            </DataTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#FF6366F1"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <Grid>
                    <Image Stretch="UniformToFill"
                           Opacity="0.7"
                           RenderOptions.BitmapScalingMode="HighQuality">
                        <Image.Source>
                            <Binding Path="." Converter="{StaticResource SkinSplashartConverter}"/>
                        </Image.Source>
                    </Image>
                    <Border Background="#80000000" VerticalAlignment="Bottom">
                        <StackPanel Margin="8,4">
                            <TextBlock Text="{Binding ChampionName}"
                                       Foreground="White"
                                       FontSize="11"
                                       FontWeight="SemiBold"
                                       TextTrimming="CharacterEllipsis"/>
                            <TextBlock Text="{Binding Name}"
                                       Foreground="#CCFFFFFF"
                                       FontSize="10"
                                       TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>
        
        <DataTemplate x:Key="ChallengeTemplate">
            <Border BorderBrush="{DynamicResource ControlStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="8"
                    Margin="4"
                    Width="140"
                    Height="140"
                    ClipToBounds="True"
                    Cursor="Hand">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background" Value="{DynamicResource CardBackgroundFillColorDefaultBrush}"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="BorderBrush" Value="#FF6366F1"/>
                                <Setter Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Border.Style>
                <StackPanel>
                    <Border Width="100" Height="100" Margin="20,10,20,0" CornerRadius="8" ClipToBounds="True">
                        <Image Source="{Binding Converter={StaticResource ChallengeIconConverter}}"
                               Stretch="UniformToFill"/>
                    </Border>
                    <TextBlock Text="{Binding Name}"
                               FontSize="11"
                               FontWeight="SemiBold"
                               TextAlignment="Center"
                               TextWrapping="Wrap"
                               Margin="8,4"
                               TextTrimming="CharacterEllipsis"
                               MaxHeight="30"/>
                </StackPanel>
            </Border>
        </DataTemplate>
    </UserControl.Resources>
    
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="16">
            <!-- Заголовок -->
            <TextBlock Text="Кастомизация профиля" 
                       FontSize="24" 
                       FontWeight="Bold" 
                       Margin="0,0,0,8"/>
            <TextBlock Text="Настройте внешний вид вашего профиля в клиенте League of Legends"
                       FontSize="12"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       TextWrapping="Wrap"
                       Margin="0,0,0,20"/>
            
            <!-- Предупреждение -->
            <Border Background="#FFFF6B6B" CornerRadius="8" Padding="12" Margin="0,0,0,24">
                <StackPanel>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,5">
                        <ui:SymbolIcon Symbol="Warning24" Foreground="White" FontSize="18" Margin="0,0,8,0"/>
                        <TextBlock Text="ПРЕДУПРЕЖДЕНИЕ" FontSize="13" FontWeight="Bold" Foreground="White"/>
                    </StackPanel>
                    <TextBlock Text="Эта функция находится в серой зоне Riot Games. Использование может повлечь блокировку аккаунта. Разработчик не несёт ответственности." 
                               TextWrapping="Wrap" Foreground="White" FontSize="12"/>
                </StackPanel>
            </Border>
            
            <!-- Статус -->
            <ui:Card Margin="0,0,0,16" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    
                    <StackPanel Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,12">
                        <TextBlock Text="Статус" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="Обход лимита символов в статусе профиля" 
                                   FontSize="11"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    
                    <ui:TextBox Grid.Row="1" Grid.Column="0"
                                Text="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.CustomStatus, Mode=TwoWay}"
                                PlaceholderText="Введите статус..."
                                Margin="0,0,8,0"/>
                    <ui:Button Grid.Row="1" Grid.Column="1"
                               Content="Применить"
                               Command="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.SetStatusCommand}"
                               IsEnabled="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.IsLoading, Converter={StaticResource InverseBoolToVisibility}}"
                               Appearance="Primary"
                               MinWidth="100"/>
                </Grid>
            </ui:Card>
            
            <!-- Бекграунд -->
            <ui:Card Margin="0,0,0,16" Padding="20">
                <StackPanel>
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Фон профиля" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="Измените фон профиля на splashart любого чемпиона" 
                                   FontSize="11"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    
                    <ui:TextBox Text="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.BackgroundSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                PlaceholderText="🔍 Поиск чемпиона..."
                                Margin="0,0,0,12"/>
                    
                    <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12">
                        <ItemsControl ItemsSource="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.FilteredSkins}"
                                      ItemTemplate="{StaticResource BackgroundSkinTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                    
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        
                        <ComboBox Grid.Column="0"
                                  ItemsSource="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.FilteredChampions}"
                                  SelectedItem="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.SelectedChampionForBackground, Mode=TwoWay}"
                                  ItemTemplate="{StaticResource ChampionTemplate}"
                                  MaxDropDownHeight="350"
                                  Margin="0,0,8,0">
                            <ComboBox.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <VirtualizingStackPanel/>
                                </ItemsPanelTemplate>
                            </ComboBox.ItemsPanel>
                        </ComboBox>
                        
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.AvailableSkins}"
                                  SelectedItem="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.SelectedSkinForBackground, Mode=TwoWay}"
                                  Margin="0,0,8,0"
                                  MaxDropDownHeight="350"
                                  IsEnabled="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.SelectedChampionForBackground, Converter={StaticResource NotNullConverter}}">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Name}"/>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        
                        <ui:Button Grid.Column="2"
                                   Content="Применить"
                                   Command="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.SetBackgroundCommand}"
                                   IsEnabled="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.IsLoading, Converter={StaticResource InverseBoolToVisibility}}"
                                   Appearance="Primary"
                                   MinWidth="100"/>
                    </Grid>
                </StackPanel>
            </ui:Card>
            
            <!-- Челенджи -->
            <ui:Card Margin="0,0,0,16" Padding="20">
                <StackPanel>
                    <StackPanel Margin="0,0,0,12">
                        <TextBlock Text="Челенджи" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="Выберите до 3 челенджей для отображения в профиле (можно выбрать одинаковые)" 
                                   FontSize="11"
                                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                   TextWrapping="Wrap"/>
                    </StackPanel>
                    
                    <ui:TextBox Text="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.ChallengeSearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                PlaceholderText="🔍 Поиск челенджа..."
                                Margin="0,0,0,12"/>
                    
                    <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto" Margin="0,0,0,12">
                        <ItemsControl ItemsSource="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.FilteredChallenges}"
                                      ItemTemplate="{StaticResource ChallengeTemplate}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <WrapPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                        </ItemsControl>
                    </ScrollViewer>
                    
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        
                        <ComboBox Grid.Column="0"
                                  ItemsSource="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.FilteredChallenges}"
                                  SelectedItem="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.SelectedChallenge1}"
                                  Margin="0,0,4,0"
                                  MaxDropDownHeight="300">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{Binding Converter={StaticResource ChallengeIconConverter}}"
                                               Width="24" Height="24" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.FilteredChallenges}"
                                  SelectedItem="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.SelectedChallenge2}"
                                  Margin="4,0,4,0"
                                  MaxDropDownHeight="300">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{Binding Converter={StaticResource ChallengeIconConverter}}"
                                               Width="24" Height="24" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                        
                        <ComboBox Grid.Column="2"
                                  ItemsSource="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.FilteredChallenges}"
                                  SelectedItem="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.SelectedChallenge3}"
                                  Margin="4,0,0,0"
                                  MaxDropDownHeight="300">
                            <ComboBox.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">
                                        <Image Source="{Binding Converter={StaticResource ChallengeIconConverter}}"
                                               Width="24" Height="24" Margin="0,0,8,0"/>
                                        <TextBlock Text="{Binding Name}" VerticalAlignment="Center"/>
                                    </StackPanel>
                                </DataTemplate>
                            </ComboBox.ItemTemplate>
                        </ComboBox>
                    </Grid>
                    
                    <ui:Button Content="Применить"
                               Command="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.SetChallengesCommand}"
                               IsEnabled="{Binding ElementName=CustomizationPageRoot, Path=ViewModel.IsLoading, Converter={StaticResource InverseBoolToVisibility}}"
                               Appearance="Primary"
                               MinWidth="100"
                               HorizontalAlignment="Left"/>
                </StackPanel>
            </ui:Card>
        </StackPanel>
    </ScrollViewer>
</UserControl>

