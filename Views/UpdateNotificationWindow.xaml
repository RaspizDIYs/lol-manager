<Window x:Class="LolManager.Views.UpdateNotificationWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        mc:Ignorable="d"
        Title="LolManager - Доступно обновление" 
        Height="200" Width="350"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        ResizeMode="NoResize"
        WindowStartupLocation="Manual"
        ShowInTaskbar="False"
        Topmost="True">
    
    <Border CornerRadius="12" 
            Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{DynamicResource ControlElevationBorderBrush}"
            BorderThickness="1"
            Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="Black" Opacity="0.3" ShadowDepth="0" BlurRadius="15"/>
        </Border.Effect>
        
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <!-- Заголовок с иконкой обновления -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,8">
                <ui:SymbolIcon Symbol="ArrowSync24" Width="20" Height="20" 
                              Foreground="{DynamicResource SystemAccentColor}" 
                              Margin="0,0,8,0"/>
                <TextBlock Text="Доступно обновление" 
                           FontWeight="SemiBold" 
                           FontSize="16"
                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
            </StackPanel>
            
            <!-- Сообщение -->
            <TextBlock Grid.Row="1" 
                       TextWrapping="Wrap"
                       FontSize="14"
                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                       Margin="0,0,0,12">
                <Run Text="Доступна новая версия "/>
                <Run Text="{Binding Version}" FontWeight="SemiBold" Foreground="{DynamicResource AccentTextFillColorPrimaryBrush}"/>
                <Run Text=" LolManager. Хотите скачать?"/>
            </TextBlock>
            
            <!-- Кнопки действий -->
            <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,8">
                <ui:Button Content="✕" 
                           Command="{Binding DismissCommand}"
                           Appearance="Secondary"
                           Width="35"
                           Height="28"
                           FontSize="14"
                           Margin="0,0,8,0"
                           ToolTip="Закрыть уведомление"/>
                <ui:Button Content="Скачать" 
                           Command="{Binding DownloadCommand}"
                           Appearance="Primary"
                           Padding="16,0"
                           Icon="{ui:SymbolIcon ArrowDownload24}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
